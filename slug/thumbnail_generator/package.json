{
  "name": "thumbnail_generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "test $(uname -s) = Darwin && node-gyp rebuild || echo 'only builds on macos'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-gyp": "^9.0.0"
  }
}
